# public/

Static assets served at the site root.

## resume.pdf — REQUIRED, not yet added

The site links to `/resume.pdf` from the Hero, the Contact section, and the
footer. **Drop the real résumé PDF here as `public/resume.pdf`.** Until then
those links resolve to a 404.

Per CLAUDE.md content rules: the PDF must not contain a phone number, street
address, or birth date.
